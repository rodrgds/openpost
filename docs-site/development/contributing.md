# Contributing

Follow the repo conventions in `AGENTS.md` and the architecture patterns already in the codebase.

## Highlights

- Use conventional commits
- Keep platform-specific code inside `internal/platform/`
- Preserve the static frontend embed flow
- Update docs when behavior or configuration changes
